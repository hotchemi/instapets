<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="hotchemi.instapets.activity.ShowPhotoActivity">

    <item
        android:id="@+id/action_refresh"
        android:icon="@drawable/navigation_refresh"
        android:orderInCategory="100"
        android:title="@string/title_activity_show_refresh"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_favorites"
        android:icon="@drawable/navigation_favorite"
        android:orderInCategory="100"
        android:title="@string/title_activity_show_favorites"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/action_settings"
        android:icon="@drawable/navigation_settings"
        android:orderInCategory="100"
        android:title="@string/title_activity_settings"
        app:showAsAction="ifRoom"/>

</menu>